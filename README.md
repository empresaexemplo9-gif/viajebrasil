# Repositório limpo

Pronto para iniciar uma **nova plataforma do zero** — outro nome e outro propósito.

O projeto anterior foi removido. O histórico permanece no git, caso seja
preciso recuperar algo:

```bash
git log --oneline           # ver commits anteriores
git checkout <commit> -- .   # restaurar arquivos de um commit
```
